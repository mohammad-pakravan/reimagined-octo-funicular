# سیستم فروش سکه (Coin Purchase System)

## نصب و راه‌اندازی

### 1. اجرای Migration

برای ایجاد جدول `coin_packages` و آپدیت جدول `payment_transactions`، فایل migration را اجرا کنید:

```bash
mysql -u [username] -p [database_name] < db/migration_add_coin_packages.sql
```

یا از طریق Docker:

```bash
docker exec -i telecaht-mysql mysql -u telecaht_user -ptelecaht_pass telecaht < db/migration_add_coin_packages.sql
```

### 2. راه‌اندازی ربات

پس از اجرای migration، ربات را مجدداً راه‌اندازی کنید:

```bash
docker-compose restart bot
```

## مدیریت پکیج‌های سکه (Admin Panel)

### دسترسی به پنل ادمین

1. به ربات پیام `/admin` بفرستید
2. روی دکمه **🪙 مدیریت پکیج‌های سکه** کلیک کنید

### ایجاد پکیج جدید

1. روی **➕ ایجاد پکیج جدید** کلیک کنید
2. مراحل زیر را دنبال کنید:
   - **نام پکیج**: مثال: "100 سکه"
   - **تعداد سکه**: مثال: 100
   - **قیمت (تومان)**: مثال: 10000
   - **تعداد استارز**: مثال: 50 (یا 0 برای غیرفعال کردن)

### مشاهده و ویرایش پکیج‌ها

1. روی **📋 لیست پکیج‌ها** کلیک کنید
2. پکیج مورد نظر را انتخاب کنید
3. می‌توانید:
   - **فعال/غیرفعال** کردن پکیج
   - **نمایش/مخفی** کردن پکیج
   - **حذف** پکیج

## نحوه خرید سکه توسط کاربران

### مسیر 1: از منوی تعامل

1. روی دکمه **🎁 تعامل** در منوی اصلی کلیک کنید
2. روی **🪙 سکه** کلیک کنید
3. روی **💳 خرید سکه** کلیک کنید
4. پکیج مورد نظر را انتخاب کنید
5. روش پرداخت را انتخاب کنید:
   - **⭐ پرداخت با استارز** (Telegram Stars)
   - **💳 پرداخت آنلاین** (زرین‌پال)

### مسیر 2: مستقیم از منوی سکه

1. روی دکمه **🎁 تعامل** در منوی اصلی کلیک کنید
2. روی **🪙 سکه** کلیک کنید
3. پکیج‌های موجود در همان صفحه نمایش داده می‌شوند
4. روی پکیج مورد نظر کلیک کنید

## روش‌های پرداخت

### 1. پرداخت با استارز (Telegram Stars)

- **مزایا**:
  - پرداخت سریع و آسان
  - بدون نیاز به خروج از تلگرام
  - تأیید خودکار پرداخت

- **نحوه کار**:
  1. کاربر روی **⭐ پرداخت با استارز** کلیک می‌کند
  2. فاکتور تلگرام نمایش داده می‌شود
  3. کاربر پرداخت را تأیید می‌کند
  4. سکه‌ها به صورت خودکار به حساب کاربر اضافه می‌شوند

### 2. پرداخت آنلاین (زرین‌پال)

- **مزایا**:
  - پرداخت با کارت بانکی
  - مناسب برای مبالغ بالاتر

- **نحوه کار**:
  1. کاربر روی **💳 پرداخت آنلاین** کلیک می‌کند
  2. لینک پرداخت نمایش داده می‌شود
  3. کاربر به درگاه زرین‌پال منتقل می‌شود
  4. پس از پرداخت موفق، سکه‌ها به حساب کاربر اضافه می‌شوند

**⚠️ نکته مهم**: کاربران باید قبل از پرداخت، فیلترشکن خود را خاموش کنند.

## تنظیمات پرداخت

### تنظیم درگاه پرداخت

برای فعال کردن پرداخت آنلاین، باید دامنه درگاه پرداخت را در تنظیمات سیستم وارد کنید:

1. به پنل ادمین بروید
2. روی **⚙️ تنظیمات سیستم** کلیک کنید
3. **payment_gateway_domain** را تنظیم کنید (مثال: `https://payment.example.com`)

## ساختار دیتابیس

### جدول `coin_packages`

```sql
CREATE TABLE coin_packages (
    id INT AUTO_INCREMENT PRIMARY KEY,
    package_name VARCHAR(200) NOT NULL,           -- نام پکیج
    coin_amount INT NOT NULL,                     -- تعداد سکه
    price FLOAT NOT NULL,                         -- قیمت به تومان
    original_price FLOAT DEFAULT NULL,            -- قیمت اصلی (قبل از تخفیف)
    discount_percent INT DEFAULT 0,               -- درصد تخفیف
    stars_required INT DEFAULT NULL,              -- تعداد استارز مورد نیاز
    payment_methods_json TEXT DEFAULT NULL,       -- روش‌های پرداخت (JSON)
    discount_start_date DATETIME DEFAULT NULL,    -- تاریخ شروع تخفیف
    discount_end_date DATETIME DEFAULT NULL,      -- تاریخ پایان تخفیف
    is_active BOOLEAN DEFAULT TRUE,               -- فعال/غیرفعال
    is_visible BOOLEAN DEFAULT TRUE,              -- نمایش/مخفی
    display_order INT DEFAULT 0,                  -- ترتیب نمایش
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

### آپدیت جدول `payment_transactions`

فیلد جدید `coin_package_id` برای ردیابی خریدهای سکه اضافه شده است.

## API Endpoints

### برای کاربران

- `points:info` - نمایش اطلاعات سکه و پکیج‌های موجود
- `points:buy` - نمایش لیست پکیج‌های خرید سکه
- `coin:package:{package_id}` - انتخاب پکیج برای خرید
- `coin:package:stars:{package_id}` - پرداخت با استارز
- `coin:package:shaparak:{package_id}` - پرداخت با زرین‌پال

### برای ادمین

- `admin:coin_packages` - منوی مدیریت پکیج‌های سکه
- `admin:coin_package:create` - ایجاد پکیج جدید
- `admin:coin_package:list` - لیست پکیج‌ها
- `admin:coin_package:view:{package_id}` - مشاهده جزئیات پکیج
- `admin:coin_package:toggle:{package_id}` - فعال/غیرفعال کردن
- `admin:coin_package:toggle_visibility:{package_id}` - نمایش/مخفی کردن
- `admin:coin_package:delete:{package_id}` - حذف پکیج

## تغییرات در منوی تعامل

### قبل از تغییرات:
```
🎁 تعامل
├── 🎁 پاداش روزانه
├── ⭐ سکه‌ها (فقط نمایش موجودی)
├── 🏅 مدال‌ها
├── 👥 دعوت دوستان
├── 📊 رتبه‌بندی
└── 🎯 ایونت‌ها
```

### بعد از تغییرات:
```
🎁 تعامل
├── 🎁 پاداش روزانه
├── 🪙 سکه (با قابلیت خرید)
│   ├── 💳 خرید سکه
│   ├── 📜 تاریخچه
│   └── 💎 تبدیل به پریمیوم
├── 👥 دعوت دوستان
└── 📊 رتبه‌بندی
```

**تغییرات اصلی:**
- ✅ نام "سکه‌ها" به "سکه" تغییر یافت
- ✅ دکمه "خرید سکه" اضافه شد
- ❌ بخش "مدال‌ها" حذف شد
- ❌ بخش "ایونت‌ها" حذف شد

## نمونه پکیج‌های پیش‌فرض

Migration به صورت خودکار 4 پکیج نمونه ایجاد می‌کند:

| پکیج | تعداد سکه | قیمت (تومان) | استارز |
|------|-----------|--------------|--------|
| 100 سکه | 100 | 10,000 | 50 ⭐ |
| 500 سکه | 500 | 45,000 | 225 ⭐ |
| 1000 سکه | 1000 | 80,000 | 400 ⭐ |
| 2500 سکه | 2500 | 180,000 | 900 ⭐ |

می‌توانید این پکیج‌ها را از پنل ادمین ویرایش یا حذف کنید.

## عیب‌یابی

### مشکل: پکیج‌ها نمایش داده نمی‌شوند

1. بررسی کنید که migration اجرا شده باشد
2. از پنل ادمین بررسی کنید که پکیج‌ها **فعال** و **قابل نمایش** باشند
3. لاگ‌های ربات را بررسی کنید

### مشکل: پرداخت آنلاین کار نمی‌کند

1. بررسی کنید که `payment_gateway_domain` در تنظیمات سیستم درست تنظیم شده باشد
2. بررسی کنید که payment server در حال اجرا باشد
3. بررسی کنید که کاربر فیلترشکن خود را خاموش کرده باشد

### مشکل: پرداخت با استارز کار نمی‌کند

1. بررسی کنید که `BOT_TOKEN` در `.env` درست تنظیم شده باشد
2. بررسی کنید که ربات توکن معتبر داشته باشد
3. بررسی کنید که `stars_required` در پکیج تنظیم شده باشد

## پشتیبانی

برای هرگونه مشکل یا سوال، لطفاً به مستندات اصلی پروژه مراجعه کنید یا با تیم پشتیبانی تماس بگیرید.

